program prueba_subprograma
    var  a,b,c : integer;
	var  d,e : float;
   
    procedure add ( x,y: in integer; result:in out integer)
	var aux, iteration : integer;
    {
        println(10);
    }

    procedure main {
        read(a); 
        read(b);
    }
