program llamada_procedimiento
    var  a,b,c : integer;
	var  d,e : float;
    procedure add ( x,y: in integer; result:in out integer)
	var aux, iteration : integer;
    {
        println(10);
    }

    procedure main {
        read(a); read(b);
        d= 1/b;
        e= 1/a;
        c= c*(c*d)+e;
        println(c*c);
    }
